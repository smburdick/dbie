extern crate grpcio;

pub mod master {

    //use super::grpcio::ChannelBuilder; // TODO does crate need to be outside? use crate::... ?
    use bitmap_vector::bitmap_vector::BitmapVector; // TODO find a way to improve this.

    // pub fn put_vector<T: ?Sized> where T: BitmapVector (vector: T) {
    //
    // }
}
